// DatoYa 2.0 — monitor de seguridad exclusivo admin
(function(){if(typeof routes==='undefined'||!routes.admin)return;const previous=routes.admin;
async function renderAdminSecurity(){if(!ME||ME.role!=='admin'){view.innerHTML='<div class="empty"><b>🛡️</b>Acceso solo para administradores.</div>';return;}let payload;try{payload=await api('/admin/security-events')}catch(e){view.innerHTML='<div class="empty">'+esc(e.message)+'</div>';return;}const events=payload.events||[],badge=r=>r>=80?'🔴 Alto':r>=40?'🟠 Medio':'🟢 Bajo';view.innerHTML='<h2 class="section-title">🛡️ Seguridad</h2><div class="card"><div class="row between"><div><b>Protección de acceso DatoYa</b><div class="small muted">Chile-only + controles de riesgo</div></div><span class="status-tag">SMS '+(payload.sms_enabled?'✅ activo':'🟡 pendiente')+'</span></div><p class="small muted">Las alertas son señales para revisión; no significan por sí solas que una persona sea fraudulenta.</p></div>'+ (events.length?events.map(x=>'<div class="card"><div class="row between"><b>'+esc(x.name||'Visitante / sin identificar')+'</b><span class="status-tag">'+badge(+x.risk)+'</span></div><div class="small">'+esc(x.event_type)+'</div><div class="small muted">'+esc(x.detail||'')+(x.ip_hint?' · IP '+esc(x.ip_hint):'')+'</div><div class="small muted">'+fmtHora(x.created_at)+'</div></div>').join(''):'<div class="empty">No hay alertas de seguridad registradas.</div>');}
routes.admin=async function(tab='dashboard'){if(tab==='seguridad')return renderAdminSecurity();return previous.apply(this,arguments);};
})();